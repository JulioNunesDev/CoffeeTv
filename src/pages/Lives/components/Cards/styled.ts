import {styled} from 'styled-components'

const Styleds = {
boxContaimer: styled.main`
width: 450px;
height: 260px;
background-image: linear-gradient(-90deg, var(--color-primary), #000);

border-radius: 8px;
`
}


export default Styleds;